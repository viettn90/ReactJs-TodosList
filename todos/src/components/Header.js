import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                <h1>TODOS LIST</h1>
            </header>
        );
    }
}

export default Header;